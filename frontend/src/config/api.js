const BASE_URL = "http://localhost:3000";

const API_URLS = {
  USERS: {
    LIST: `${BASE_URL}/users`,
  },
  BOOKS: {
    LIST: `${BASE_URL}/books`,
  },
  LOANS: {
    LIST: `${BASE_URL}/loans`,
  },
};

export default API_URLS;
